<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zero.eprocurement.app.user.mapper.UserMapper">

	<!-- 查询用户列表信息-->
   	<select id="queryUserList" parameterType="UserQueryDto" resultType="UserResultDto">
		select id, name, age, create_by createBy, create_time createTime, update_by updateBy, update_time updateTime from user
	</select>
	
</mapper>
